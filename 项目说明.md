# 项目说明

## 项目概述

四足机器狗（带机械臂与气泵）上位机 ROS2 工作空间。下位机已完成四腿运动学/步态控制、机械臂动作执行、气泵开关，上下位机通过 USB CDC 虚拟串口全双工通信。

**上位机职责**：视觉识别（YOLO 目标检测/3D定位）、坐标系转换、串口通信桥接、高层路径规划与策略执行。

## 技术栈

- **ROS2**: Humble / Iron / Jazzy
- **C++17**: 策略控制节点（quadruped_control/strategy_manager）
- **Python 3**: 视觉检测节点 + 串口桥 + Launch 脚本
- **串口通信**: Linux termios（arm_serial_bridge），Windows 下开发用 pytest
- **构建工具**: colcon (--symlink-install)
- **3D 目标检测**: OpenVINO INT8 YOLOv8n + 点云定位（detection_3d）
- **待集成**: Nav2, BehaviorTree.CPP

## 目录结构

```
gsing_dog/
├── src/
│   ├── quadruped_interfaces/   # 自定义 msg 定义 (CMake)
│   │   ├── msg/
│   │   │   ├── LegCommand.msg       # 四腿控制指令
│   │   │   ├── ArmPumpCommand.msg   # 机械臂 + 气泵指令
│   │   │   └── RobotState.msg       # 机器狗完整状态反馈
│   │   ├── CMakeLists.txt
│   │   └── package.xml
│   │
│   ├── quadruped_control/      # 策略控制节点 (C++)
│   │   ├── src/
│   │   │   └── strategy_manager_node.cpp   # 状态机 + 策略协调 (100Hz)
│   │   ├── CMakeLists.txt
│   │   └── package.xml
│   │
│   ├── quadruped_bringup/      # Launch 脚本 + 配置 (Python)
│   │   ├── launch/
│   │   │   └── robot.launch.py         # 启动 strategy_manager
│   │   ├── config/
│   │   │   └── params.yaml
│   │   ├── setup.py
│   │   └── package.xml
│   │
│   └── detection_3d/           # YOLO 3D 目标检测 + 统一串口桥 (Python)
│       ├── detection_3d/
│       │   ├── protocol.py              # 0x55 0xAA 协议打包/解析
│       │   ├── geometry.py              # 像素 → 3D 投影
│       │   ├── target_filter.py         # EMA + 稳定滤波
│       │   ├── place_targets.py         # 放置坐标校验
│       │   ├── yolo_detector_node.py    # OpenVINO INT8 检测
│       │   ├── detection_3d_calculator_node.py  # 3D 坐标计算
│       │   ├── arm_serial_bridge_node.py        # 统一串口桥
│       │   ├── coordinate_sample_logger_node.py # 3D 坐标采样记录
│       │   ├── estimator_comparison_logger_node.py # 同帧算法 A/B 采样
│       │   ├── compare_positions_csv.py         # 3D 坐标 A/B 对比
│       │   └── depth_algorithm_benchmark.py     # 深度估计算法 A/B benchmark
│       ├── launch/
│       ├── config/
│       ├── models/
│       ├── test/                        # pytest 50 个单测
│       ├── setup.py
│       └── package.xml
│
├── arm_stm/                 # 下位机固件
├── 项目说明.md
├── 下位机串口协议对接说明.md
├── CLAUDE.md
└── .gitignore
```

## 协议

上下位机统一走 `0x55 0xAA` 帧格式，定义在 [protocol.py](src/detection_3d/detection_3d/protocol.py)。下位机固件修改细节见 [下位机串口协议对接说明.md](下位机串口协议对接说明.md)。

### Host → MCU

| func_id | 名称 | payload |
|---------|------|---------|
| `0x12` | 机械臂目标 | `u8 target_type + 3×f32 (x,y,z)`，`target_type=0` 抓取/`1` 放置，坐标为 `arm_base`，单位 m |
| `0x13` | 气泵开关 | `u8 pump_on`，`1` 开启气泵/`0` 关闭气泵 |

### MCU → Host

| func_id | 名称 | payload |
|---------|------|---------|
| `0x21` | 机械臂反馈 | `u8 arm_state + 3×f32 end_xyz + f32 theta1_rad`，末端坐标为 `arm_base`，单位 m |

## 运行方式

### 编译

```bash
cd /path/to/gsing_dog
colcon build --symlink-install
source install/setup.bash
```

### 启动

```bash
# 策略节点
ros2 launch quadruped_bringup robot.launch.py

# 视觉检测 + 串口桥
ros2 launch detection_3d bringup.launch.py use_arm_bridge:=true

# Linux 小主机一键启动：Astra 相机 + YOLO 3D 检测 + STM32 串口桥
ros2 launch detection_3d vision_grasp_system.launch.py
```

`vision_grasp_system.launch.py` 默认启用 Astra `depth_registration=true` 和 `color_depth_synchronization=true`，RGB UVC 使用 `uvc_product_id=0x0511`，STM32 CDC 默认使用 `/dev/serial/by-id/usb-STMicroelectronics_STM32_Virtual_ComPort_3542354B3333-if00`。现场微调 RGB 检测框到深度图的残余对齐误差时，可临时传入：

```bash
ros2 launch detection_3d vision_grasp_system.launch.py depth_pixel_offset_x_px:=0.0 depth_pixel_offset_y_px:=0.0
```

### Topic 拓扑

```
yolo_detector ──Detection2D──▶ detection_3d_calculator ──Detection3D──▶ arm_serial_bridge ──坐标变换──▶ 0x12/0x13 ──▶ MCU
arm_serial_bridge ◀──0x21──  MCU
```

`arm_serial_bridge` 检测到稳定目标后只缓存相机坐标系坐标；每个发送周期读取最新 MCU 反馈的末端坐标和 `theta1_rad`，再按 `vision_transform.c` 的公式实时转换成 `arm_base` 抓取目标并打包 0x12。放置目标 `place_targets_m` 已是 `arm_base` 固定坐标，不再套相机变换。最终发给 MCU 前会对抓取/放置目标统一做机械补偿，默认 `command_offset_z_m=0.09`，该补偿不改变视觉解算坐标。当前相机安装偏移参数默认：

- `camera_offset_x_m: 0.105`
- `camera_offset_y_m: 0.0`
- `camera_offset_z_m: -0.078`

Host 状态机为 `WAIT_DETECTION → SEND_GRASP → GRASP_DELAY → SEND_PLACE → PLACE_DELAY → WAIT_DETECTION`。到达判断由 Host 使用 MCU 实时反馈的末端坐标完成，默认 `reach_tolerance_m=0.015`、`reach_stable_frames=3`。抓取阶段只使用最新有效 3D 检测，超过 `detection_timeout_sec=0.5` 秒没有新目标时清空抓取目标并回到等待检测，不继续发送历史缓存坐标。到达抓取目标后先发送 `0x13 pump_on=1`，成功后保持抓取目标并延时 `arrival_delay_sec=1.0` 秒；到达放置目标后先发送 `0x13 pump_on=0`，成功后保持放置目标并延时 1 秒。反馈超过 `feedback_timeout_sec=0.5` 秒时不发送抓取目标。`serial_tx_log=true` 时终端会输出 `SERIAL_TX 0x12 ARM_TARGET ...` 和 `SERIAL_TX 0x13 PUMP ...`，其中坐标是最终写入串口的补偿后坐标。

### 3D 坐标稳定性/准确性评估

```bash
# 1. 先用合成深度 ROI 对比旧中心法和当前深度簇法
benchmark_depth_estimator

# 2. 启动检测链路并记录稳定 3D 检测坐标
ros2 launch detection_3d bringup.launch.py use_sample_logger:=true sample_output_path:=samples.csv

# 3. 如需实机 A/B，对同一摆放位置分别采集旧中心法和当前深度簇法
ros2 launch detection_3d bringup.launch.py use_sample_logger:=true sample_output_path:=baseline.csv depth_estimator_mode:=center_median
ros2 launch detection_3d bringup.launch.py use_sample_logger:=true sample_output_path:=optimized.csv depth_estimator_mode:=cluster_centroid

# 4. 更严格的实机 A/B：同一帧同时记录旧中心法和当前深度簇法
ros2 launch detection_3d bringup.launch.py use_estimator_comparison_logger:=true estimator_comparison_output_path:=paired.csv

# 5. 用尺量/标定得到的目标真值计算稳定性和准确性指标
evaluate_positions samples.csv --ground-truth 0.120 0.035 0.480
evaluate_positions baseline.csv --ground-truth 0.120 0.035 0.480
evaluate_positions optimized.csv --ground-truth 0.120 0.035 0.480
compare_positions baseline.csv optimized.csv --ground-truth 0.120 0.035 0.480
compare_positions paired.csv --baseline-columns baseline_x baseline_y baseline_z --candidate-columns optimized_x optimized_y optimized_z --ground-truth 0.120 0.035 0.480
compare_positions paired.csv --baseline-columns baseline_x baseline_y baseline_z --candidate-columns optimized_x optimized_y optimized_z --ground-truth 0.120 0.035 0.480 --min-stability-improvement-pct 20 --min-rmse-improvement-pct 20
```

合成 benchmark 用于证明算法思路优于旧中心法；实机最终仍以采样 CSV 为准。输出指标中，`max_std_m` 用于判断坐标稳定性，`rmse_m`/`mean_error_m`/`max_error_m` 用于判断坐标准确性。

建议实机验证口径：

- 固定相机、方块和光照，单次采样至少保留 30 行 paired CSV。
- `ground-truth` 使用尺量/标定得到的相机坐标系真值，单位为 m。
- `compare_positions` 返回码为 0，且 `optimized` 的 `max_std_m`、`rmse_m` 均低于 `baseline`，才认为当前算法在该场景下通过验证。
- 如需定义“明显更好”，建议先用 `--min-stability-improvement-pct 20 --min-rmse-improvement-pct 20`，后续可按机械臂抓取容差收紧。

---

## 变更日志

| 日期 | 改动内容 | 原因 |
|------|---------|------|
| 2026-05-25 | 初始化项目 | 创建 ROS2 工作空间框架 |
| 2026-05-25 | 重构 detection_3d 包 | 拆分纯逻辑模块、参数化、可测试 |
| 2026-05-25 | 统一上下位机协议 | 走一套 0x55 0xAA，Python bridge 为唯一串口桥 |
| 2026-05-25 | 0x12 帧增加 flag 字节，MCU 通过 0x21 控制抓放 | 抓取发相机坐标，放置发预设坐标，FK 由下位机做 |
| 2026-05-25 | 清理死代码：删除 serial_bridge_node.cpp、quadruped_vision、astra_camera、astra_camera_msgs 及对应 launch/config 引用 | 去除未使用/已替代的包和文件 |
| 2026-05-25 | 简化协议：0x12 改为纯相机坐标(去掉 flag 字节)，0x21 改为单字节 flag | MCU 自行判断抓/放模式，上位机只发坐标 |
| 2026-05-31 | 检测模型切换为 OpenVINO INT8 | N100 无独显，OpenVINO INT8 CPU 推理速度优于 PyTorch；类别名更新为 Cube_food/Cube_ins/Cube_medicine/Cube_tool |
| 2026-06-03 | 修正自适应 ROI 文档描述 | 与按检测框较小边计算 ROI 的实际算法保持一致 |
| 2026-06-04 | 优化 3D 目标点估计为最近可靠深度簇质心投影 | 减少检测框中心偏移、中心深度空洞、背景深度占多数、RGB/深度分辨率差异、时间同步过宽和稳定阈值过松导致的坐标误差 |
| 2026-06-04 | 新增 3D 坐标评估指标模块 | 为实机验证稳定性标准差和坐标准确性误差提供可测试计算工具 |
| 2026-06-04 | 新增 evaluate_positions CSV 评估命令 | 支持用实机采样坐标文件复现稳定性/准确性指标 |
| 2026-06-04 | 新增 coordinate_sample_logger 采样节点 | 将稳定 3D 检测坐标记录为 CSV，闭环实机稳定性/准确性验证 |
| 2026-06-04 | bringup 增加 use_sample_logger 可选采样开关 | 方便实机验证时同步启动检测链路和坐标采样 |
| 2026-06-04 | 新增 benchmark_depth_estimator 合成对比命令 | 在同一批深度 ROI 上量化旧中心法和当前深度簇法的稳定性、准确性差异 |
| 2026-06-04 | 新增 depth_estimator_mode 算法切换参数 | 支持实机采集旧中心法与当前深度簇法的 A/B 对比数据 |
| 2026-06-04 | 新增 compare_positions CSV 对比命令 | 直接输出 baseline 与 optimized 坐标稳定性、准确性提升百分比 |
| 2026-06-04 | compare_positions 支持同一 CSV 内两组坐标列 | 方便后续用同帧数据做 baseline/optimized 配对验证 |
| 2026-06-04 | 新增 estimator_comparison_logger 同帧对比采样节点 | 直接从同一深度帧记录旧中心法与当前深度簇法坐标，减少两次采样带来的场景漂移 |
| 2026-06-04 | compare_positions 新增最小提升阈值判定 | 支持用命令返回码自动判断稳定性和准确性提升是否达标 |
| 2026-06-04 | 深度簇质心增加最大连通区域过滤 | 避免同深度但空间分离的小噪声块拉偏目标点中心 |
| 2026-06-04 | 修正深度簇选择需满足最大连通块阈值 | 防止多个离散近深度噪声点靠总数误判为可靠目标 |
| 2026-06-04 | 合成 benchmark 增加同深度空间噪声场景 | 让稳定性/准确性证明覆盖目标偏移、背景占多数和分离噪声共同存在的情况 |
| 2026-06-04 | 补充实机验证判定口径 | 明确 paired CSV 样本量、ground-truth、返回码和最小提升阈值要求 |
| 2026-06-29 | 上位机发送前增加相机坐标到 arm_base 坐标转换 | 0x12 必须发送转换后的机械臂基座坐标，与给定 vision_transform.c 公式一致 |
| 2026-06-29 | 重构 arm_serial_bridge 协议与抓放状态机 | Host 发送 `target_type+arm_base xyz`，MCU 实时反馈末端坐标和 `theta1`，由 Host 判断到达并切换抓取/放置 |
| 2026-06-29 | arm_serial_bridge 增加 feedback 读取重连 | 避免 MCU 晚枚举或 USB CDC 临时断开后上位机一直等待反馈 |
| 2026-06-29 | 新增下位机串口协议对接说明 | 方便下位机负责人按新 `0x12/0x21` 协议、状态反馈和坐标单位修改固件 |
| 2026-06-29 | 增加 0x13 气泵开关协议 | 抓取到达后先开泵再延时，放置到达后先关泵再延时 |
| 2026-06-29 | 抓取发送禁止使用过期视觉坐标 | 目标丢失时停止发送抓取坐标，避免历史缓存坐标干扰下位机 |
| 2026-06-30 | 发送前 Z 轴机械补偿调整为 `+0.09m` | 在原 `+0.15m` 基础上额外下调 `0.06m`，适配机械实测偏差 |
| 2026-06-30 | 新增 Linux 小主机一键启动 launch | 将 Astra 相机、YOLO 3D 检测和 STM32 串口桥封装成一条 `ros2 launch` 命令 |
| 2026-06-30 | 增加 RGB 到深度图像素偏移校准参数 | 通过 `depth_pixel_offset_x_px/y_px` 微调残余 RGB/Depth 对齐误差，提高实机坐标准确性 |
| 2026-06-30 | 串口发送增加 `SERIAL_TX` 终端日志 | 直接打印最终写入 MCU 的目标坐标和气泵状态，方便 SSH 调试 |
