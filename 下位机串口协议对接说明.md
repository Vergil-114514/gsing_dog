# 下位机串口协议对接说明

本文档给下位机固件负责人使用，描述当前上位机 `arm_serial_bridge` 期望的 USB CDC 串口协议和抓放配合逻辑。

## 总体职责

- Host 负责视觉检测、相机坐标到机械臂基座坐标的转换、抓取/放置状态机、到达判断。
- MCU 负责持续执行最近收到的机械臂目标坐标，并实时反馈机械臂末端坐标、`theta1` 和机械臂状态。
- Host 发给 MCU 的目标坐标均为 `arm_base` 坐标系，单位 m。
- MCU 发给 Host 的末端坐标也必须是 `arm_base` 坐标系，单位 m。
- 角度单位统一为 rad。

## 统一帧格式

所有方向使用同一帧格式：

```text
[0x55][0xAA][func_id][payload_len][payload...][checksum]
```

规则：

- 多字节数值全部小端序。
- `float` 为 IEEE754 `float32`。
- `payload_len` 只统计 payload 字节数，不包含帧头和校验。
- `checksum = 从 0x55 到 payload 最后一个字节的累加和 & 0xFF`。
- 最短帧长度为 5 字节：2 字节 header + 1 字节 func_id + 1 字节 payload_len + 1 字节 checksum。

## Host 到 MCU：机械臂目标

```text
func_id = 0x12
name = ARM_TARGET
payload_len = 13
frame_len = 18
payload = u8 target_type + f32 target_x + f32 target_y + f32 target_z
```

字段表：

| 偏移 | 类型 | 名称 | 说明 |
|---:|---|---|---|
| 0 | `u8` | `target_type` | `0` 抓取目标，`1` 放置目标 |
| 1 | `f32` | `target_x` | `arm_base` x，单位 m |
| 5 | `f32` | `target_y` | `arm_base` y，单位 m |
| 9 | `f32` | `target_z` | `arm_base` z，单位 m |

MCU 行为建议：

- 收到 `target_type=0`：按抓取目标执行，可开启抓取相关动作逻辑。
- 收到 `target_type=1`：按放置目标执行，可开启放置相关动作逻辑。
- Host 会在对应状态内周期性重复发送同一个或更新后的目标，MCU 应以“最近一次有效目标”为准。
- 如果 `target_type` 不是 `0/1`，建议丢弃该帧，并可在反馈中置 `arm_state=3`。

示例帧：

```text
# grasp: target_type=0, xyz=(0.1, -0.2, 0.3)
55 AA 12 0D 00 CD CC CC 3D CD CC 4C BE 9A 99 99 3E 6D

# place: target_type=1, xyz=(-0.1, 0.111, 0.42)
55 AA 12 0D 01 CD CC CC BD F8 53 E3 3D 3D 0A D7 3E 08
```

## Host 到 MCU：气泵开关

```text
func_id = 0x13
name = PUMP_CONTROL
payload_len = 1
frame_len = 6
payload = u8 pump_on
```

字段表：

| 偏移 | 类型 | 名称 | 说明 |
|---:|---|---|---|
| 0 | `u8` | `pump_on` | `1` 开启气泵，`0` 关闭气泵 |

MCU 行为建议：

- 收到 `pump_on=1`：立即开启气泵/负压吸附。
- 收到 `pump_on=0`：立即关闭气泵/释放方块。
- 如果 `pump_on` 不是 `0/1`，建议丢弃该帧，并可在反馈中置 `arm_state=3`。
- Host 只在进入抓取延时、放置延时前发送泵状态；如果串口发送失败，Host 会在后续 timer tick 重试。

示例帧：

```text
# pump on
55 AA 13 01 01 14

# pump off
55 AA 13 01 00 13
```

## MCU 到 Host：机械臂实时反馈

```text
func_id = 0x21
name = ARM_FEEDBACK
payload_len = 17
frame_len = 22
payload = u8 arm_state + f32 end_x + f32 end_y + f32 end_z + f32 theta1_rad
```

字段表：

| 偏移 | 类型 | 名称 | 说明 |
|---:|---|---|---|
| 0 | `u8` | `arm_state` | `0` idle/ready，`1` moving，`2` reached/holding，`3` error |
| 1 | `f32` | `end_x` | 当前末端 `arm_base` x，单位 m |
| 5 | `f32` | `end_y` | 当前末端 `arm_base` y，单位 m |
| 9 | `f32` | `end_z` | 当前末端 `arm_base` z，单位 m |
| 13 | `f32` | `theta1_rad` | 当前机械臂底座/第一关节角，单位 rad |

MCU 行为要求：

- 建议至少 10 Hz 周期发送 feedback；更高频率也可以。
- Host 超过 `feedback_timeout_sec=0.5` 秒收不到 feedback 时，不会发送抓取目标。
- `end_x/end_y/end_z` 必须是机械臂末端在 `arm_base` 坐标系下的实时坐标。
- `theta1_rad` 必须与 `vision_transform.c` 中的 `angles->theta1_geo` 含义一致。
- `arm_state=3` 表示错误；Host 收到后会停止继续发送目标。

示例帧：

```text
# moving: state=1, end=(0.1, -0.2, 0.3), theta1=1.57
55 AA 21 11 01 CD CC CC 3D CD CC 4C BE 9A 99 99 3E C3 F5 C8 3F 40

# error: state=3, end=(0, 0, 0), theta1=0
55 AA 21 11 03 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 34
```

## Host 状态机

Host 当前状态机如下：

```text
WAIT_DETECTION
  等待稳定视觉目标。

SEND_GRASP
  周期性读取最新 MCU feedback。
  用稳定相机目标 + feedback.end_xyz + feedback.theta1 实时转换为 arm_base 抓取目标。
  周期性发送 0x12 target_type=0。
  Host 判断末端到达最近一次发送的抓取目标后，进入 GRASP_DELAY。

GRASP_DELAY
  先发送 0x13 pump_on=1。
  pump_on=1 发送成功后开始 1 秒延时。
  期间继续发送最后一次抓取目标，用于保持。
  结束后进入 SEND_PLACE。

SEND_PLACE
  周期性发送固定放置坐标，0x12 target_type=1。
  Host 判断末端到达放置坐标后，进入 PLACE_DELAY。

PLACE_DELAY
  先发送 0x13 pump_on=0。
  pump_on=0 发送成功后开始 1 秒延时。
  期间继续发送放置目标，用于保持。
  结束后清空视觉目标和滤波器，回到 WAIT_DETECTION。
```

Host 到达判断：

```text
distance(feedback.end_xyz, last_sent_target_xyz) <= reach_tolerance_m
连续 reach_stable_frames 次成立才算到达
```

当前默认参数：

```text
reach_tolerance_m = 0.015
reach_stable_frames = 3
arrival_delay_sec = 1.0
feedback_timeout_sec = 0.5
```

注意：Host 不再使用 MCU 发来的“抓取/放置 flag”切状态；`0x21` 必须改为实时 feedback。

## 坐标转换边界

上位机发送抓取目标前会按下列公式将相机坐标转换为 `arm_base` 坐标：

```text
x_e = cam_y + CAMERA_OFFSET_X
y_e = cam_x + CAMERA_OFFSET_Y
z_e = -cam_z + CAMERA_OFFSET_Z

arm_x = x_e*cos(theta1) - y_e*sin(theta1) + current_end_x
arm_y = x_e*sin(theta1) + y_e*cos(theta1) + current_end_y
arm_z = z_e + current_end_z
```

其中：

- `cam_x/cam_y/cam_z` 来自视觉检测，相机坐标系，单位 m。
- `CAMERA_OFFSET_X/Y/Z` 是相机安装偏移，由上位机配置。
- `theta1`、`current_end_x/y/z` 来自 MCU 最新 `0x21` feedback。
- 放置目标已经是 `arm_base` 固定坐标，不再做相机转换。

## MCU 解析伪代码

```c
bool parse_frame(uint8_t *buf, size_t len) {
    if (len < 5) return false;
    if (buf[0] != 0x55 || buf[1] != 0xAA) return false;

    uint8_t func_id = buf[2];
    uint8_t payload_len = buf[3];
    size_t frame_len = 5 + payload_len;
    if (len < frame_len) return false;

    uint8_t sum = 0;
    for (size_t i = 0; i < frame_len - 1; ++i) {
        sum += buf[i];
    }
    if (sum != buf[frame_len - 1]) return false;

    uint8_t *payload = &buf[4];
    if (func_id == 0x12 && payload_len == 13) {
        uint8_t target_type = payload[0];
        float x = read_f32_le(&payload[1]);
        float y = read_f32_le(&payload[5]);
        float z = read_f32_le(&payload[9]);
        handle_arm_target(target_type, x, y, z);
    } else if (func_id == 0x13 && payload_len == 1) {
        uint8_t pump_on = payload[0];
        handle_pump_control(pump_on);
    }
    return true;
}
```

## 联调检查表

- MCU 能正确从乱序/粘包串口流中搜索 `0x55 0xAA` 帧头。
- MCU 能校验 checksum，错误帧丢弃。
- MCU 能解析 `0x12` 的 13 字节 payload。
- MCU 能解析 `0x13` 的 1 字节 payload，并按 `pump_on=1/0` 开关气泵。
- MCU 周期发送 `0x21` 的 17 字节 payload。
- Host 启动后 0.5 秒内能收到 feedback；否则不会发送抓取目标。
- Host 发送抓取时，MCU 能看到 `target_type=0`。
- Host 发送放置时，MCU 能看到 `target_type=1`。
- 抓取目标到达后，MCU 能看到 `0x13 pump_on=1`，随后 Host 才开始 1 秒抓取延时。
- 放置目标到达后，MCU 能看到 `0x13 pump_on=0`，随后 Host 才开始 1 秒放置延时。
- MCU 报 `arm_state=3` 后，Host 停止发送目标。
- MCU 的 `end_xyz` 与 Host 发送目标使用同一 `arm_base` 坐标系和 m 单位。
- MCU 的 `theta1_rad` 与 `vision_transform.c` 里的 `theta1_geo` 方向和零位一致。
