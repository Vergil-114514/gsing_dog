# 下位机串口协议对接说明

本文档给下位机固件负责人使用，描述当前上位机 `arm_serial_bridge` 期望的 USB CDC 串口协议和抓放配合逻辑。

## 总体职责

- Host 负责视觉检测、相机坐标到机械臂基座坐标的转换、抓取/放置状态机、到达判断。
- MCU 负责持续执行最近收到的机械臂目标坐标，并实时反馈机械臂末端坐标、`theta1` 和机械臂状态。
- Host 发给 MCU 的目标坐标均为 `arm_base` 坐标系，单位 m。
- MCU 发给 Host 的末端坐标也必须是 `arm_base` 坐标系，单位 m。
- 角度单位统一为 rad。

## 统一帧格式

所有方向使用同一帧格式：

```text
[0x55][0xAA][func_id][payload_len][payload...][checksum]
```

规则：

- 多字节数值全部小端序。
- `float` 为 IEEE754 `float32`。
- `payload_len` 只统计 payload 字节数，不包含帧头和校验。
- `checksum = 从 0x55 到 payload 最后一个字节的累加和 & 0xFF`。
- 最短帧长度为 5 字节：2 字节 header + 1 字节 func_id + 1 字节 payload_len + 1 字节 checksum。

## Host 到 MCU：机械臂目标

```text
func_id = 0x12
name = ARM_TARGET
payload_len = 13
frame_len = 18
payload = u8 target_type + f32 target_x + f32 target_y + f32 target_z
```

字段表：

| 偏移 | 类型 | 名称 | 说明 |
|---:|---|---|---|
| 0 | `u8` | `target_type` | `0` 抓取目标，`1` 放置目标 |
| 1 | `f32` | `target_x` | `arm_base` x，单位 m |
| 5 | `f32` | `target_y` | `arm_base` y，单位 m |
| 9 | `f32` | `target_z` | `arm_base` z，单位 m |

MCU 行为建议：

- 收到 `target_type=0`：按抓取目标执行，可开启抓取相关动作逻辑。
- 收到 `target_type=1`：按放置目标执行，可开启放置相关动作逻辑。
- Host 会在对应状态内周期性重复发送同一个或更新后的目标，MCU 应以“最近一次有效目标”为准。
- 如果 `target_type` 不是 `0/1`，建议丢弃该帧；不要依赖 `arm_state=3` 影响 Host 流程。
- 抓取目标已由 Host 转换到 `arm_base`，并已叠加抓取用机械补偿。
- 抓取末段相机被机械臂遮挡时，Host 会继续重复发送最近几帧抓取目标的滤波坐标；MCU 无需区分该情况。
- 放置目标是实测最终坐标，Host 不再叠加抓取用 `command_offset_z_m` 等补偿。

当前固定放置目标：

| `place_target_index` | 位置 | MCU 收到的 `target_xyz`，单位 m |
|---:|---|---|
| 0 | 右后 | `(-0.2470, -0.2000, 0.3835)` |
| 1 | 左后 | `(-0.2000, 0.2140, 0.3850)` |
| 2 | 左前 | `(0.2407, 0.21014, 0.3840)` |
| 3 | 右前 | `(0.2320, -0.2240, 0.3875)` |

示例帧：

```text
# grasp: target_type=0, xyz=(0.1, -0.2, 0.3)
55 AA 12 0D 00 CD CC CC 3D CD CC 4C BE 9A 99 99 3E 6D

# place index=0 右后: target_type=1, xyz=(-0.2470, -0.2000, 0.3835)
55 AA 12 0D 01 91 ED 7C BE CD CC 4C BE 1D 5A C4 3E F3
```

## Host 到 MCU：气泵开关

```text
func_id = 0x13
name = PUMP_CONTROL
payload_len = 1
frame_len = 6
payload = u8 pump_on
```

字段表：

| 偏移 | 类型 | 名称 | 说明 |
|---:|---|---|---|
| 0 | `u8` | `pump_on` | `1` 开启气泵，`0` 关闭气泵 |

MCU 行为建议：

- 收到 `pump_on=1`：立即开启气泵/负压吸附。
- 收到 `pump_on=0`：立即关闭气泵/释放方块。
- 如果 `pump_on` 不是 `0/1`，建议丢弃该帧；不要依赖 `arm_state=3` 影响 Host 流程。
- Host 在抓取到达后发送 `pump_on=1`，发送成功后才开始抓取延时；放置到达后先保持放置目标 1 秒，再发送 `pump_on=0`。如果串口发送失败，Host 会在后续 timer tick 重试。

示例帧：

```text
# pump on
55 AA 13 01 01 14

# pump off
55 AA 13 01 00 13
```

## MCU 到 Host：机械臂实时反馈

```text
func_id = 0x21
name = ARM_FEEDBACK
payload_len = 17
frame_len = 22
payload = u8 arm_state + f32 end_x + f32 end_y + f32 end_z + f32 theta1_rad
```

字段表：

| 偏移 | 类型 | 名称 | 说明 |
|---:|---|---|---|
| 0 | `u8` | `arm_state` | `0` idle/ready，`1` moving，`2` reached/holding，`3` 保留；Host 只把受约束的目标发送后 `2` 作为到达条件之一 |
| 1 | `f32` | `end_x` | 当前末端 `arm_base` x，单位 m |
| 5 | `f32` | `end_y` | 当前末端 `arm_base` y，单位 m |
| 9 | `f32` | `end_z` | 当前末端 `arm_base` z，单位 m |
| 13 | `f32` | `theta1_rad` | 当前机械臂底座/第一关节角，单位 rad |

MCU 行为要求：

- 建议至少 10 Hz 周期发送 feedback；更高频率也可以。
- Host 超过 `feedback_timeout_sec=0.5` 秒收不到 feedback 时，不会发送抓取目标；非等待状态下超过 `feedback_loss_abort_sec=3.0` 秒收不到 feedback 时，Host 会尝试发送 `0x13 pump_on=0` 并回到等待检测。
- `end_x/end_y/end_z` 必须是机械臂末端在 `arm_base` 坐标系下的实时坐标。
- `theta1_rad` 必须与 `vision_transform.c` 中的 `angles->theta1_geo` 含义一致。
- MCU 可以在确认当前目标已到位/保持时上报 `arm_state=2`；Host 只在 feedback 时间晚于当前目标发送时间、连续满足、距离目标不远且末端确实移动过时，才把 `arm_state=2` 作为到达触发条件之一。
- `arm_state` 仅作为普通状态信息保留，Host 不因为 `arm_state=3` 停止发送目标或气泵命令。
- 下位机建议不要再把普通执行异常、限位、轨迹误差等情况上报为会影响流程的 error；Host 会根据实时末端坐标 `end_x/end_y/end_z` 和目标发送后的 `arm_state=2` 判断到达。

示例帧：

```text
# moving: state=1, end=(0.1, -0.2, 0.3), theta1=1.57
55 AA 21 11 01 CD CC CC 3D CD CC 4C BE 9A 99 99 3E C3 F5 C8 3F 40

# reserved state=3, end=(0, 0, 0), theta1=0
55 AA 21 11 03 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 34
```

## Host 状态机

Host 当前状态机如下：

```text
WAIT_DETECTION
  等待稳定视觉目标。

SEND_GRASP
  周期性读取最新 MCU feedback。
  用稳定相机目标 + feedback.end_xyz + feedback.theta1 实时转换为 arm_base 抓取目标。
  周期性发送 0x12 target_type=0。
  Host 判断末端到达最近一次发送的抓取目标后，进入 GRASP_DELAY。

GRASP_DELAY
  先发送 0x13 pump_on=1。
  pump_on=1 发送成功后开始 1 秒延时。
  期间继续发送最后一次抓取目标，用于保持。
  结束后进入 SEND_PLACE。

SEND_PLACE
  周期性发送固定放置坐标，0x12 target_type=1。
  Host 判断末端到达放置坐标后，进入 PLACE_DELAY。

PLACE_DELAY
  先成功发送一次 place_hold 放置目标，再开始 1 秒延时。
  1 秒延时结束后发送 0x13 pump_on=0。
  期间继续发送放置目标，用于保持。
  结束后清空视觉目标和滤波器，回到 WAIT_DETECTION。
```

Host 到达判断：

```text
feedback 新鲜，且满足以下任一条件：

1. distance(feedback.end_xyz, last_sent_target_xyz) <= reach_tolerance_m
   连续 reach_stable_frames 次成立

2. distance(feedback.end_xyz, last_sent_target_xyz) <= arrival_stall_max_distance_m
   且连续 arrival_stall_frames 次末端移动量 <= arrival_stall_epsilon_m

3. arm_state == 2，且 feedback 时间晚于当前目标成功发送时间
   且连续 mcu_reached_stable_frames 次成立
   且 distance(feedback.end_xyz, last_sent_target_xyz) <= mcu_reached_max_distance_m
   且目标发送后末端移动量 >= mcu_reached_min_motion_m
      或已经满足普通距离到达
```

当前默认参数：

```text
reach_tolerance_m = 0.020
reach_stable_frames = 2
arrival_stall_epsilon_m = 0.015
arrival_stall_frames = 5
arrival_stall_max_distance_m = 0.08
mcu_reached_stable_frames = 2
mcu_reached_max_distance_m = 0.10
mcu_reached_min_motion_m = 0.01
arrival_delay_sec = 1.0
feedback_timeout_sec = 0.5
feedback_loss_abort_sec = 3.0
grasp_command_filter_window = 5
grasp_occlusion_timeout_sec = 8.0
```

注意：Host 不再使用 MCU 发来的“抓取/放置 flag”切状态；`0x21` 必须改为实时 feedback。`arm_state=2` 只是辅助到达信息，Host 会用时间、距离、运动量和连续帧做保护，避免旧 reached 状态误触发。MCU 只需要执行最近一次目标并反馈末端实时坐标。抓取末段视觉遮挡保持完全由 Host 处理，下位机继续执行最近收到的 `0x12` 目标即可。

## 坐标转换边界

上位机发送抓取目标前会按相机前倾角将相机坐标转换为 `arm_base` 坐标。当前默认 `camera_tilt_forward_deg=45.0`，正值表示相机光轴从竖直向下朝机械臂局部 `+x_e` 前倾：

```text
alpha = radians(camera_tilt_forward_deg)

x_e = cam_y*cos(alpha) + cam_z*sin(alpha) + CAMERA_OFFSET_X
y_e = cam_x + CAMERA_OFFSET_Y
z_e = cam_y*sin(alpha) - cam_z*cos(alpha) + CAMERA_OFFSET_Z

arm_x = x_e*cos(theta1) - y_e*sin(theta1) + current_end_x
arm_y = x_e*sin(theta1) + y_e*cos(theta1) + current_end_y
arm_z = z_e + current_end_z
```

其中：

- `cam_x/cam_y/cam_z` 来自视觉检测，相机坐标系，单位 m。
- `CAMERA_OFFSET_X/Y/Z` 是相机安装偏移，由上位机配置。
- `theta1`、`current_end_x/y/z` 来自 MCU 最新 `0x21` feedback。
- 抓取目标转换后还会叠加 Host 侧抓取机械补偿，当前默认 `command_offset_z_m=0.15`，`command_abs_y_offset_m=-0.01`。
- 放置目标已经是 `arm_base` 固定最终坐标，不做相机转换，也不叠加 `command_offset_z_m`。

## MCU 解析伪代码

```c
bool parse_frame(uint8_t *buf, size_t len) {
    if (len < 5) return false;
    if (buf[0] != 0x55 || buf[1] != 0xAA) return false;

    uint8_t func_id = buf[2];
    uint8_t payload_len = buf[3];
    size_t frame_len = 5 + payload_len;
    if (len < frame_len) return false;

    uint8_t sum = 0;
    for (size_t i = 0; i < frame_len - 1; ++i) {
        sum += buf[i];
    }
    if (sum != buf[frame_len - 1]) return false;

    uint8_t *payload = &buf[4];
    if (func_id == 0x12 && payload_len == 13) {
        uint8_t target_type = payload[0];
        float x = read_f32_le(&payload[1]);
        float y = read_f32_le(&payload[5]);
        float z = read_f32_le(&payload[9]);
        handle_arm_target(target_type, x, y, z);
    } else if (func_id == 0x13 && payload_len == 1) {
        uint8_t pump_on = payload[0];
        handle_pump_control(pump_on);
    }
    return true;
}
```

## 联调检查表

- MCU 能正确从乱序/粘包串口流中搜索 `0x55 0xAA` 帧头。
- MCU 能校验 checksum，错误帧丢弃。
- MCU 能解析 `0x12` 的 13 字节 payload。
- MCU 能解析 `0x13` 的 1 字节 payload，并按 `pump_on=1/0` 开关气泵。
- MCU 周期发送 `0x21` 的 17 字节 payload。
- Host 启动后 0.5 秒内能收到 feedback；否则不会发送抓取目标。
- Host 发送抓取时，MCU 能看到 `target_type=0`。
- Host 发送放置时，MCU 能看到 `target_type=1`，且坐标等于上表 4 个固定放置目标之一。
- 抓取目标到达后，MCU 能看到 `0x13 pump_on=1`，随后 Host 才开始 1 秒抓取延时。
- 放置目标到达后，Host 会继续保持放置目标约 1 秒，随后 MCU 才会看到 `0x13 pump_on=0` 释放方块。
- MCU 报 `arm_state=3` 不会让 Host 停止发送目标；下位机不要用该字段控制上位机流程。
- MCU 的 `end_xyz` 与 Host 发送目标使用同一 `arm_base` 坐标系和 m 单位。
- MCU 的 `theta1_rad` 与 `vision_transform.c` 里的 `theta1_geo` 方向和零位一致。
